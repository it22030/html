#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define ll long long
#define all(v) (v).begin(), (v).end()
#define endl '\n'

int main() {
    fastio;
    
    int t;
    cin >> t;
    while (t--) {  }

    return 0;
}